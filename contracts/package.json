{
  "name": "@turing-arena/contracts",
  "version": "1.0.0",
  "description": "Smart contracts for TuringArena on Mantle",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network mantleTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network mantleMainnet",
    "verify": "hardhat verify --network mantleTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.6.1",
    "hardhat": "^2.19.5"
  }
}
